"""
Model exported as python.
Name : climate_vulnerability_model
Group : 
With QGIS : 33411
"""

from qgis.core import QgsProcessing
from qgis.core import QgsProcessingAlgorithm
from qgis.core import QgsProcessingMultiStepFeedback
from qgis.core import QgsProcessingParameterVectorLayer
from qgis.core import QgsProcessingParameterFeatureSink
import processing


class Model_reda(QgsProcessingAlgorithm):

    def initAlgorithm(self, config=None):
        self.addParameter(QgsProcessingParameterVectorLayer('dworzec', 'dworzec', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('granice_miasta', 'Granice miasta', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('kuko', 'KUKO', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('kuos', 'KUOS', types=[QgsProcessing.TypeVectorPolygon], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('kupg', 'KUPG', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('kuza', 'KUZA', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ochrona_zdrowia', 'ochrona_zdrowia', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ptlz', 'PTLZ', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ptnz', 'PTNZ', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ptpl', 'PTPL', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ptrk', 'PTRK', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('pttr', 'PTTR', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ptut', 'PTUT', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ptzb', 'PTZB', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('ratunkowe', 'Ratunkowe', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('skdr', 'SKDR', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('sktr2', 'SKTR', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('supr', 'SUPR', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('swrs', 'SWRS', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterVectorLayer('urzdy', 'Urzędy', types=[QgsProcessing.TypeVectorAnyGeometry], defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Drogi_glowne', 'drogi_glowne', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Obiekty_tech', 'obiekty_tech', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Siatka_iloczyn', 'siatka_iloczyn', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, supportsAppend=True, defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Siatka_uslugi', 'siatka_uslugi', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, supportsAppend=True, defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Siatka_wrazliwosc', 'siatka_wrazliwosc', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, supportsAppend=True, defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_kuko', 'iloczyn_kuko', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_kuko.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_sktr', 'iloczyn_SKTR', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_sktr.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_supr', 'iloczyn_SUPR', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_supr.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_swrs', 'iloczyn_SWRS', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_drogi', 'iloczyn_drogi', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_drogi.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_jednorodzinna', 'iloczyn_jednorodzinna', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_jednorodzinna.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_pozostala', 'iloczyn_pozostala', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_pozostala.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_przemyslowa', 'iloczyn_przemyslowa', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_przemyslowa.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_wielorodzinna', 'iloczyn_wielorodzinna', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_wielorodzinna.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_uslugowa', 'iloczyn_uslugowa', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_uslugowa.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_urzad', 'iloczyn_urzad', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_urzad.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_oswiata', 'iloczyn_oswiata', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue=None))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_ochrona_zdrowia', 'iloczyn_ochrona_zdrowia', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_ochrona_zdrowia.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_dworzec', 'iloczyn_dworzec', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_dworzec.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_ratunkowe', 'iloczyn_ratunkowe', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_ratunkowe.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_zabud_techn', 'iloczyn_zabud_techn', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_ratunkowe.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_kupg', 'iloczyn_KUPG', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_kupg.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_kuza', 'iloczyn_KUZA', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_kuza.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_ptlz', 'iloczyn_PTLZ', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_ptlz.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_ptpl', 'iloczyn_PTPL', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_ptpl.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_ptrk', 'iloczyn_PTRK', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_ptrk.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_pttr', 'iloczyn_PTTR', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_pttr.shp'))
        self.addParameter(QgsProcessingParameterFeatureSink('Iloczyn_ptut', 'iloczyn_PTUT', type=QgsProcessing.TypeVectorAnyGeometry, createByDefault=True, defaultValue='C:/Users/klaudia.chrusciel/Desktop/GIS podyplomowe/Reda wrażliwość/reda_model/iloczyn_ptut.shp'))

    def processAlgorithm(self, parameters, context, model_feedback):
        # Use a multi-step feedback, so that individual child algorithm progress reports are adjusted for the
        # overall progress through the model
        feedback = QgsProcessingMultiStepFeedback(80, model_feedback)
        results = {}
        outputs = {}

        # Zmień nazwę pola2
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['kuko'],
            'NEW_NAME': 'parking_stacja',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola2'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(1)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola12
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['swrs'],
            'NEW_NAME': 'cieki',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola12'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(2)
        if feedback.isCanceled():
            return {}

        # Wyodrębnij za pomocą wyrażenia4
        alg_params = {
            'EXPRESSION': '"RODZAJ" = \'przemysłowo-składowa\'',
            'INPUT': parameters['ptzb'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['WyodrbnijZaPomocWyraenia4'] = processing.run('native:extractbyexpression', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(3)
        if feedback.isCanceled():
            return {}

        # Zapisz obiekty wektorowe do pliku4
        alg_params = {
            'ACTION_ON_EXISTING_FILE': 0,  # stwórz lub nadpisz plik
            'DATASOURCE_OPTIONS': '',
            'INPUT': outputs['WyodrbnijZaPomocWyraenia4']['OUTPUT'],
            'LAYER_NAME': '',
            'LAYER_OPTIONS': '',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZapiszObiektyWektoroweDoPliku4'] = processing.run('native:savefeatures', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(4)
        if feedback.isCanceled():
            return {}

        # Wyodrębnij za pomocą wyrażenia7
        alg_params = {
            'EXPRESSION': '"RODZAJ" IN (\'elektrociepłownia\', \'przepompownia\', \'podstacja elektroenergetyczna\') ',
            'INPUT': parameters['kupg'],
            'OUTPUT': parameters['Obiekty_tech']
        }
        outputs['WyodrbnijZaPomocWyraenia7'] = processing.run('native:extractbyexpression', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Obiekty_tech'] = outputs['WyodrbnijZaPomocWyraenia7']['OUTPUT']

        feedback.setCurrentStep(5)
        if feedback.isCanceled():
            return {}

        # Wyodrębnij za pomocą wyrażenia2
        alg_params = {
            'EXPRESSION': '"RODZAJ" = \'jednorodzinna\'',
            'INPUT': parameters['ptzb'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['WyodrbnijZaPomocWyraenia2'] = processing.run('native:extractbyexpression', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(6)
        if feedback.isCanceled():
            return {}

        # Zapisz obiekty wektorowe do pliku7
        alg_params = {
            'ACTION_ON_EXISTING_FILE': 0,  # stwórz lub nadpisz plik
            'DATASOURCE_OPTIONS': '',
            'INPUT': outputs['WyodrbnijZaPomocWyraenia7']['OUTPUT'],
            'LAYER_NAME': '',
            'LAYER_OPTIONS': '',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZapiszObiektyWektoroweDoPliku7'] = processing.run('native:savefeatures', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(7)
        if feedback.isCanceled():
            return {}

        # Utwórz siatkę
        alg_params = {
            'CRS': 'ProjectCrs',
            'EXTENT': '451999.375500000,462072.655600000,747162.594400000,754854.858100000 [EPSG:2180]',
            'HOVERLAY': 0,
            'HSPACING': 200,
            'TYPE': 4,  # Sześciokąt (poligon)
            'VOVERLAY': 0,
            'VSPACING': 200,
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['UtwrzSiatk'] = processing.run('native:creategrid', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(8)
        if feedback.isCanceled():
            return {}

        # Wyodrębnij za pomocą wyrażenia6
        alg_params = {
            'EXPRESSION': '"RODZAJ" = \'handlowo-usługowa\'',
            'INPUT': parameters['ptzb'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['WyodrbnijZaPomocWyraenia6'] = processing.run('native:extractbyexpression', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(9)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola1
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['kuos'],
            'NEW_NAME': 'oswiata',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola1'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(10)
        if feedback.isCanceled():
            return {}

        # Zapisz obiekty wektorowe do pliku2
        alg_params = {
            'ACTION_ON_EXISTING_FILE': 0,  # stwórz lub nadpisz plik
            'DATASOURCE_OPTIONS': '',
            'INPUT': outputs['WyodrbnijZaPomocWyraenia2']['OUTPUT'],
            'LAYER_NAME': '',
            'LAYER_OPTIONS': '',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZapiszObiektyWektoroweDoPliku2'] = processing.run('native:savefeatures', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(11)
        if feedback.isCanceled():
            return {}

        # Wyodrębnij za pomocą wyrażenia
        alg_params = {
            'EXPRESSION': '"KLASA_DROG" = \'droga główna\'',
            'INPUT': parameters['skdr'],
            'OUTPUT': parameters['Drogi_glowne']
        }
        outputs['WyodrbnijZaPomocWyraenia'] = processing.run('native:extractbyexpression', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Drogi_glowne'] = outputs['WyodrbnijZaPomocWyraenia']['OUTPUT']

        feedback.setCurrentStep(12)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola4
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['kuza'],
            'NEW_NAME': 'zabytki',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola4'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(13)
        if feedback.isCanceled():
            return {}

        # Przytnij
        alg_params = {
            'INPUT': outputs['UtwrzSiatk']['OUTPUT'],
            'OVERLAY': parameters['granice_miasta'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['Przytnij'] = processing.run('native:clip', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(14)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola7
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['ptrk'],
            'NEW_NAME': 'krzewy',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola7'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(15)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola8
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['pttr'],
            'NEW_NAME': 'uprawy',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola8'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(16)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola3
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['kupg'],
            'NEW_NAME': 'infra_techniczna',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola3'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(17)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola9
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['ptut'],
            'NEW_NAME': 'sady_rod',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola9'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(18)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola11
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['supr'],
            'NEW_NAME': 'infra_podziemna',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola11'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(19)
        if feedback.isCanceled():
            return {}

        # Napraw geometrie3
        alg_params = {
            'INPUT': outputs['ZapiszObiektyWektoroweDoPliku2']['OUTPUT'],
            'METHOD': 1,  # strukturalna
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['NaprawGeometrie3'] = processing.run('native:fixgeometries', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(20)
        if feedback.isCanceled():
            return {}

        # Wyodrębnij za pomocą wyrażenia5
        alg_params = {
            'EXPRESSION': '"RODZAJ" = \'wielorodzinna\'',
            'INPUT': parameters['ptzb'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['WyodrbnijZaPomocWyraenia5'] = processing.run('native:extractbyexpression', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(21)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola10
        alg_params = {
            'FIELD': 'R_POJ_SZYN',
            'INPUT': parameters['sktr2'],
            'NEW_NAME': 'kolej',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola10'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(22)
        if feedback.isCanceled():
            return {}

        # Wyodrębnij za pomocą wyrażenia3
        alg_params = {
            'EXPRESSION': '"RODZAJ" = \'pozostała zabudowa\'',
            'INPUT': parameters['ptzb'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['WyodrbnijZaPomocWyraenia3'] = processing.run('native:extractbyexpression', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(23)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola5
        alg_params = {
            'FIELD': 'RODZAJ',
            'INPUT': parameters['ptlz'],
            'NEW_NAME': 'lasy',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola5'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(24)
        if feedback.isCanceled():
            return {}

        # Zmień nazwę pola6
        alg_params = {
            'FIELD': 'MATE_NAWIE',
            'INPUT': parameters['ptpl'],
            'NEW_NAME': 'plac',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZmieNazwPola6'] = processing.run('native:renametablefield', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(25)
        if feedback.isCanceled():
            return {}

        # Napraw geometrie1
        alg_params = {
            'INPUT': outputs['ZmieNazwPola8']['OUTPUT'],
            'METHOD': 1,  # strukturalna
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['NaprawGeometrie1'] = processing.run('native:fixgeometries', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(26)
        if feedback.isCanceled():
            return {}

        # Zapisz obiekty wektorowe do pliku6
        alg_params = {
            'ACTION_ON_EXISTING_FILE': 0,  # stwórz lub nadpisz plik
            'DATASOURCE_OPTIONS': '',
            'INPUT': outputs['WyodrbnijZaPomocWyraenia6']['OUTPUT'],
            'LAYER_NAME': '',
            'LAYER_OPTIONS': '',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZapiszObiektyWektoroweDoPliku6'] = processing.run('native:savefeatures', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(27)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól1
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'pow_kwa',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': '$area/10000',
            'INPUT': outputs['Przytnij']['OUTPUT'],
            'OUTPUT': parameters['Siatka_iloczyn']
        }
        outputs['KalkulatorPl1'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Siatka_iloczyn'] = outputs['KalkulatorPl1']['OUTPUT']

        feedback.setCurrentStep(28)
        if feedback.isCanceled():
            return {}

        # Iloczyn12
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola12']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_swrs']
        }
        outputs['Iloczyn12'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_swrs'] = outputs['Iloczyn12']['OUTPUT']

        feedback.setCurrentStep(29)
        if feedback.isCanceled():
            return {}

        # Iloczyn19
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': parameters['urzdy'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_urzad']
        }
        outputs['Iloczyn19'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_urzad'] = outputs['Iloczyn19']['OUTPUT']

        feedback.setCurrentStep(30)
        if feedback.isCanceled():
            return {}

        # Iloczyn20
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': parameters['ochrona_zdrowia'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_ochrona_zdrowia']
        }
        outputs['Iloczyn20'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_ochrona_zdrowia'] = outputs['Iloczyn20']['OUTPUT']

        feedback.setCurrentStep(31)
        if feedback.isCanceled():
            return {}

        # Iloczyn6
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola6']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_ptpl']
        }
        outputs['Iloczyn6'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_ptpl'] = outputs['Iloczyn6']['OUTPUT']

        feedback.setCurrentStep(32)
        if feedback.isCanceled():
            return {}

        # Iloczyn4
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola4']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_kuza']
        }
        outputs['Iloczyn4'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_kuza'] = outputs['Iloczyn4']['OUTPUT']

        feedback.setCurrentStep(33)
        if feedback.isCanceled():
            return {}

        # Iloczyn16
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZapiszObiektyWektoroweDoPliku4']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_przemyslowa']
        }
        outputs['Iloczyn16'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_przemyslowa'] = outputs['Iloczyn16']['OUTPUT']

        feedback.setCurrentStep(34)
        if feedback.isCanceled():
            return {}

        # Napraw geometrie2
        alg_params = {
            'INPUT': outputs['ZmieNazwPola7']['OUTPUT'],
            'METHOD': 1,  # strukturalna
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['NaprawGeometrie2'] = processing.run('native:fixgeometries', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(35)
        if feedback.isCanceled():
            return {}

        # Zapisz obiekty wektorowe do pliku
        alg_params = {
            'ACTION_ON_EXISTING_FILE': 0,  # stwórz lub nadpisz plik
            'DATASOURCE_OPTIONS': '',
            'INPUT': outputs['WyodrbnijZaPomocWyraenia']['OUTPUT'],
            'LAYER_NAME': '',
            'LAYER_OPTIONS': '',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZapiszObiektyWektoroweDoPliku'] = processing.run('native:savefeatures', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(36)
        if feedback.isCanceled():
            return {}

        # Napraw geometrie
        alg_params = {
            'INPUT': outputs['ZmieNazwPola5']['OUTPUT'],
            'METHOD': 1,  # strukturalna
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['NaprawGeometrie'] = processing.run('native:fixgeometries', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(37)
        if feedback.isCanceled():
            return {}

        # Iloczyn8
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['NaprawGeometrie1']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_pttr']
        }
        outputs['Iloczyn8'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_pttr'] = outputs['Iloczyn8']['OUTPUT']

        feedback.setCurrentStep(38)
        if feedback.isCanceled():
            return {}

        # Iloczyn18
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZapiszObiektyWektoroweDoPliku6']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_uslugowa']
        }
        outputs['Iloczyn18'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_uslugowa'] = outputs['Iloczyn18']['OUTPUT']

        feedback.setCurrentStep(39)
        if feedback.isCanceled():
            return {}

        # Iloczyn1
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola1']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_oswiata']
        }
        outputs['Iloczyn1'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_oswiata'] = outputs['Iloczyn1']['OUTPUT']

        feedback.setCurrentStep(40)
        if feedback.isCanceled():
            return {}

        # Zapisz obiekty wektorowe do pliku5
        alg_params = {
            'ACTION_ON_EXISTING_FILE': 0,  # stwórz lub nadpisz plik
            'DATASOURCE_OPTIONS': '',
            'INPUT': outputs['WyodrbnijZaPomocWyraenia5']['OUTPUT'],
            'LAYER_NAME': '',
            'LAYER_OPTIONS': '',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZapiszObiektyWektoroweDoPliku5'] = processing.run('native:savefeatures', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(41)
        if feedback.isCanceled():
            return {}

        # Iloczyn17
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZapiszObiektyWektoroweDoPliku5']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_wielorodzinna']
        }
        outputs['Iloczyn17'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_wielorodzinna'] = outputs['Iloczyn17']['OUTPUT']

        feedback.setCurrentStep(42)
        if feedback.isCanceled():
            return {}

        # Iloczyn3
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZapiszObiektyWektoroweDoPliku7']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_kupg']
        }
        outputs['Iloczyn3'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_kupg'] = outputs['Iloczyn3']['OUTPUT']

        feedback.setCurrentStep(43)
        if feedback.isCanceled():
            return {}

        # Iloczyn22
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': parameters['ratunkowe'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_ratunkowe']
        }
        outputs['Iloczyn22'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_ratunkowe'] = outputs['Iloczyn22']['OUTPUT']

        feedback.setCurrentStep(44)
        if feedback.isCanceled():
            return {}

        # Iloczyn10
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola10']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_sktr']
        }
        outputs['Iloczyn10'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_sktr'] = outputs['Iloczyn10']['OUTPUT']

        feedback.setCurrentStep(45)
        if feedback.isCanceled():
            return {}

        # Iloczyn11
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola11']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_supr']
        }
        outputs['Iloczyn11'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_supr'] = outputs['Iloczyn11']['OUTPUT']

        feedback.setCurrentStep(46)
        if feedback.isCanceled():
            return {}

        # Zapisz obiekty wektorowe do pliku3
        alg_params = {
            'ACTION_ON_EXISTING_FILE': 0,  # stwórz lub nadpisz plik
            'DATASOURCE_OPTIONS': '',
            'INPUT': outputs['WyodrbnijZaPomocWyraenia3']['OUTPUT'],
            'LAYER_NAME': '',
            'LAYER_OPTIONS': '',
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['ZapiszObiektyWektoroweDoPliku3'] = processing.run('native:savefeatures', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(47)
        if feedback.isCanceled():
            return {}

        # Iloczyn7
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['NaprawGeometrie2']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_ptrk']
        }
        outputs['Iloczyn7'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_ptrk'] = outputs['Iloczyn7']['OUTPUT']

        feedback.setCurrentStep(48)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól20
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'urzad',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn19_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl1']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl20'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(49)
        if feedback.isCanceled():
            return {}

        # Iloczyn21
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': parameters['dworzec'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_dworzec']
        }
        outputs['Iloczyn21'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_dworzec'] = outputs['Iloczyn21']['OUTPUT']

        feedback.setCurrentStep(50)
        if feedback.isCanceled():
            return {}

        # Iloczyn23
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': parameters['ptnz'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_zabud_techn']
        }
        outputs['Iloczyn23'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_zabud_techn'] = outputs['Iloczyn23']['OUTPUT']

        feedback.setCurrentStep(51)
        if feedback.isCanceled():
            return {}

        # Iloczyn2
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola2']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_kuko']
        }
        outputs['Iloczyn2'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_kuko'] = outputs['Iloczyn2']['OUTPUT']

        feedback.setCurrentStep(52)
        if feedback.isCanceled():
            return {}

        # Iloczyn9
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZmieNazwPola9']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_ptut']
        }
        outputs['Iloczyn9'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_ptut'] = outputs['Iloczyn9']['OUTPUT']

        feedback.setCurrentStep(53)
        if feedback.isCanceled():
            return {}

        # Iloczyn15
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZapiszObiektyWektoroweDoPliku3']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_pozostala']
        }
        outputs['Iloczyn15'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_pozostala'] = outputs['Iloczyn15']['OUTPUT']

        feedback.setCurrentStep(54)
        if feedback.isCanceled():
            return {}

        # Iloczyn14
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['NaprawGeometrie3']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_jednorodzinna']
        }
        outputs['Iloczyn14'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_jednorodzinna'] = outputs['Iloczyn14']['OUTPUT']

        feedback.setCurrentStep(55)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól21
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'ochr_zdrow',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn20_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl20']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl21'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(56)
        if feedback.isCanceled():
            return {}

        # Iloczyn13
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['ZapiszObiektyWektoroweDoPliku']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_drogi']
        }
        outputs['Iloczyn13'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_drogi'] = outputs['Iloczyn13']['OUTPUT']

        feedback.setCurrentStep(57)
        if feedback.isCanceled():
            return {}

        # Iloczyn5
        alg_params = {
            'GRID_SIZE': None,
            'INPUT': outputs['NaprawGeometrie']['OUTPUT'],
            'INPUT_FIELDS': [''],
            'OVERLAY': outputs['KalkulatorPl1']['OUTPUT'],
            'OVERLAY_FIELDS': [''],
            'OVERLAY_FIELDS_PREFIX': '',
            'OUTPUT': parameters['Iloczyn_ptlz']
        }
        outputs['Iloczyn5'] = processing.run('native:intersection', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Iloczyn_ptlz'] = outputs['Iloczyn5']['OUTPUT']

        feedback.setCurrentStep(58)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól22
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'dworzec',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn21_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl21']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl22'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(59)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól23
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'ratunkowe',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn22_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl22']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl23'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(60)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól24
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'zab_techni',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn23_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl23']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl24'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(61)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól2
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'kolej',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': "overlay_intersects( @Iloczyn10_OUTPUT,  $length , limit:=1, sort_by_intersection_size:='des')[0]",
            'INPUT': outputs['KalkulatorPl24']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl2'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(62)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól3
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'oswiata',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn1_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl2']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl3'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(63)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól4
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'stapali_pa',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn2_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl3']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl4'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(64)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól5
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'obie_techn',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn3_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl4']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl5'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(65)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól6
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'zabytki',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn4_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl5']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl6'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(66)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól7
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'lasy',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn5_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl6']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl7'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(67)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól8
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'plac',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn6_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl7']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl8'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(68)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól9
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'krzewy',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn7_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl8']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl9'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(69)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól10
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'roslin_nis',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn8_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl9']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl10'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(70)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól11
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'sady_rod',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn9_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl10']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl11'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(71)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól12
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'infr_podzi',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': "overlay_intersects( @Iloczyn11_OUTPUT,  $length , limit:=1, sort_by_intersection_size:='des')[0]",
            'INPUT': outputs['KalkulatorPl11']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl12'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(72)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól13
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'cieki',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': "overlay_intersects( @Iloczyn12_OUTPUT,  $length , limit:=1, sort_by_intersection_size:='des')[0]",
            'INPUT': outputs['KalkulatorPl12']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl13'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(73)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól14
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'drogi',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': "overlay_intersects( @Iloczyn13_OUTPUT,  $length , limit:=1, sort_by_intersection_size:='des')[0]",
            'INPUT': outputs['KalkulatorPl13']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl14'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(74)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól15
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'jedonordzi',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn14_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl14']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl15'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(75)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól16
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'zab_pozost',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn15_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl15']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl16'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(76)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól17
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'zab_przemy',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn16_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl16']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl17'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(77)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól18
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'wielorodzi',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn17_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl17']['OUTPUT'],
            'OUTPUT': QgsProcessing.TEMPORARY_OUTPUT
        }
        outputs['KalkulatorPl18'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)

        feedback.setCurrentStep(78)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól19
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'uslugi',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'overlay_intersects( @Iloczyn18_OUTPUT , $area*100, limit:=1, sort_by_intersection_size:=\'des\')[0] / "pow_kwa" / 10000',
            'INPUT': outputs['KalkulatorPl18']['OUTPUT'],
            'OUTPUT': parameters['Siatka_uslugi']
        }
        outputs['KalkulatorPl19'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Siatka_uslugi'] = outputs['KalkulatorPl19']['OUTPUT']

        feedback.setCurrentStep(79)
        if feedback.isCanceled():
            return {}

        # Kalkulator pól25
        alg_params = {
            'FIELD_LENGTH': 0,
            'FIELD_NAME': 'wrazliwosc',
            'FIELD_PRECISION': 0,
            'FIELD_TYPE': 0,  # Liczba dziesiętna (double)
            'FORMULA': 'CASE\r\n\r\n  WHEN "ochr_zdrow" >= 1 OR "urzad" >= 1 OR "dworzec" >= 1 OR "zabytki" >= 1 OR \r\n       "oswiata" >= 1 OR "ratunkowe" >= 1 OR "obie_techn" >= 1 OR\r\n       "uslugi" >= 50 OR "wielorodzi" >= 50 OR "zab_przemy" >= 50 OR \r\n       "stapali_pa" >= 50 OR "zab_techni" >= 50 OR "plac" >= 50 THEN 4\r\n\r\n  WHEN ("uslugi" >= 1 AND "uslugi" < 50) OR \r\n       ("wielorodzi" >= 1 AND "wielorodzi" < 50) OR \r\n       ("zab_przemy" >= 1 AND "zab_przemy" < 50) OR \r\n       ("stapali_pa" >= 1 AND "stapali_pa" < 50) OR \r\n       ("zab_techni" >= 1 AND "zab_techni" < 50) OR \r\n       ("plac" >= 1 AND "plac" < 50) THEN\r\n    CASE\r\n      WHEN "kolej" >= 1 OR "infr_podzi" >= 1 OR "drogi" >= 1 THEN 4\r\n      ELSE 3\r\n    END\r\n\r\n  WHEN "jedonordzi" >= 50 OR "zab_pozost" >= 50 OR \r\n       ("sady_rod" >= 1 AND "sady_rod" < 50) OR \r\n       ("lasy" >= 1 AND "lasy" < 50) OR \r\n       ("krzewy" >= 1 AND "krzewy" < 50) OR \r\n       ("roslin_nis" >= 1 AND "roslin_nis" < 50) THEN\r\n    CASE\r\n      WHEN "kolej" >= 1 OR "infr_podzi" >= 1 OR "drogi" >= 1 THEN 3\r\n      ELSE 2\r\n    END\r\n\r\n  WHEN "sady_rod" >= 50 OR "lasy" >= 50 OR "krzewy" >= 50 OR "roslin_nis" >= 50 OR \r\n       "cieki" >= 1 OR \r\n       ("jedonordzi" >= 1 AND "jedonordzi" < 50) OR \r\n       ("zab_pozost" >= 1 AND "zab_pozost" < 50) THEN 1\r\n\r\n  ELSE 1\r\nEND\r\n\r\n\r\n',
            'INPUT': outputs['KalkulatorPl19']['OUTPUT'],
            'OUTPUT': parameters['Siatka_wrazliwosc']
        }
        outputs['KalkulatorPl25'] = processing.run('native:fieldcalculator', alg_params, context=context, feedback=feedback, is_child_algorithm=True)
        results['Siatka_wrazliwosc'] = outputs['KalkulatorPl25']['OUTPUT']
        return results

    def name(self):
        return 'model_reda'

    def displayName(self):
        return 'model_reda'

    def group(self):
        return ''

    def groupId(self):
        return ''

    def createInstance(self):
        return Model_reda()
